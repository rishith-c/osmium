/* ===========================================================================
 * shell.h - Interactive Command Shell
 * ===========================================================================
 * The main user interface for Dual-State OS. Reads commands from the
 * keyboard and dispatches them to built-in handlers.
 * =========================================================================== */

#ifndef SHELL_H
#define SHELL_H

/* Start the interactive shell (does not return) */
void shell_run(void);

#endif /* SHELL_H */
